Feature: Admin catalog cover cache clear
Status: Complete
Started: 2026-06-03
Completed: 2026-06-03

Ralph loop:
- Baseline before edits: previous loop completed with clean worktree and `pnpm run verify` passing.
- RED: Added admin controller delegation test for `clearCache()` and cache service test proving cover cache files are removed with aggregate result counts.
- GREEN: Added `POST /api/v1/admin/warehouse/cache/clear`, shared `WarehouseCacheClearResult` type, and `WarehouseCatalogCoverCacheService.clear()`.
- Hardening: Clear result returns only aggregate cleared entry/byte counts plus post-clear aggregate status.
- Review checkpoint: Privacy/spec reviewer approved admin-only route coverage and aggregate-only response shape.
- Review checkpoint: Code-quality reviewer found a clear/write race; fixed cache writes to return sanitized live bytes when persistence fails and added filesystem regression coverage.
- Focused verification: `pnpm --filter server exec vitest run src/modules/warehouse/warehouse-admin.controller.test.ts src/modules/warehouse/warehouse-catalog-cover-cache.service.test.ts` passed.
- Type verification: `pnpm run typecheck` passed.
- Lint verification: `pnpm --filter server lint:check` passed.

Notes:
- The clear action deletes only the catalog cover-cache root under app data.
- It does not expose source keys, paths, remote ids, file names, hashes, content types, or credentials.
- In-flight cover responses remain readable if a clear removes the cache target while persistence is attempted.
