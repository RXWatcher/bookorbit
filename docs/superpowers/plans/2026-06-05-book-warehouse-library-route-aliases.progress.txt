2026-06-05 05:45:54 EDT - Reproduced a Book Warehouse library URL alias leak at the router normalization seam: string `-1` normalized to `ebooks`, but numeric `-1` returned no redirect. Patched the router param reader to accept numeric params and added a regression test so internal source-backed ids stay internal while user-facing library URLs remain `/library/ebooks` and `/library/audiobooks`.
2026-06-05 07:45:24 EDT - Extended the route alias fix to source-backed item detail and reader consumers. Friendly native library routes like `/library/ebooks/items/:remoteId` and `/read/library/audiobooks/items/:remoteId` now resolve media type through the shared library route parser instead of leaking the internal virtual ids into user-facing paths.
